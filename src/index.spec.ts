// Import supertest from 'supertest';
// import {server} from './index';

// describe('server', () => {
// 	const request = supertest.agent(server);

// 	afterAll((done) => {
// 		server.close(done);
// 	});

// 	it('should get /', async () => {
// 		const response = await request.get('/');

// 		expect(response.status).toBe(200);
// 		expect(response.body).toEqual({data: 'It works!'});
// 	});
// });
